package com.ndb.blocker

/**
 * Centralized configuration constants.
 * Device ID is the Firebase RTDB node key — change here to affect all components.
 */
object NdbConfig {
    const val DEVICE_ID = "android_pixel"
}
