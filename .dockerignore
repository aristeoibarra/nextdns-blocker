# NextDNS Blocker - Docker Ignore
#
# This file applies to PRODUCTION builds (Dockerfile).
# For DEVELOPMENT builds (Dockerfile.dev), src/ and tests/ are mounted
# as volumes, so these exclusions don't affect the dev workflow.

# Git
.git
.gitignore
.github

# Python
__pycache__
*.py[cod]
*$py.class
*.so
.Python
*.egg-info
.eggs
*.egg
.pytest_cache
.coverage
htmlcov
.tox
.nox
.mypy_cache
.ruff_cache

# Virtual environments
venv
.venv
env
.env.local

# IDE
.idea
.vscode
*.swp
*.swo
*~

# Tests (excluded from production, mounted as volume in dev)
tests/
pytest.ini

# Documentation (excluded from production)
*.md
!README.md
docs/

# Build artifacts
dist/
build/
*.spec

# Local configuration (mounted as volume)
config.json
.env

# Development files (not needed in production image)
Dockerfile.dev
docker-compose.dev.yml
Makefile
CONTRIBUTING.md
CHANGELOG.md

# Misc
.DS_Store
Thumbs.db
*.log
